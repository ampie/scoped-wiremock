apply plugin: 'groovy'
dependencies {
    compileOnly('javax.enterprise:cdi-api:1.2')
    compile(project(':scoped-wiremock-integration-common'))
    compile(project(':scoped-wiremock-integration-jaxws'))
    compile(project(':scoped-wiremock-integration-filter'))
    testCompile 'org.codehaus.groovy:groovy-all:2.4.5'
    testCompile('javax.enterprise:cdi-api:1.2')
    testCompile("org.spockframework:spock-core:0.7-groovy-2.0") {
        exclude module: "groovy-all"
    }
    testCompile("com.github.goldin:spock-extensions:0.1.4") {
        exclude module: "spock-core"
        exclude module: "slf4j-api"
    }
    testCompile('cglib:cglib-nodep:2.2')
}